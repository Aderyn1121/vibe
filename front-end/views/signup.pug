include utils
include head

.nav-bar
    .nav-bar__brand
      #nav-logo.wrapper
        include svg/logo.html
      .brand__vibe VIBE
h1 EDIT TO SHOW REGISTER
.main
  .text
    p To coninue, log in to Vibe
  button#facebook-button CONTINUE WITH FACEBOOK
  button#apple-button CONTINUE WITH APPLE
  button#phone-button CONTINUE WITH PHONE NUMBER
  .line
    .line__one
    .line__two
    #orText OR
  form(method="POST" action='/')#login-form
    +input('_csrf', 'hidden')
    +input('email-username', 'text', 'Email or username')
    +input('password', 'password', 'Password')
    .submit
      .remember
        +input('remember', 'checkbox')
        label(for="remember") Remember me?
      button#login-button Login
  .text
    a(href="") Forgot your password?
  .line
    .line__one
    .line__two
  .text
    p Don't have an account?
  button#signup-button SIGN UP FOR VIBE
